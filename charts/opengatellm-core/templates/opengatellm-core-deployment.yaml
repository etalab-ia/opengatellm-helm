apiVersion: apps/v1
kind: Deployment
metadata:
  name: opengatellm-core
  namespace: {{ default .Release.Namespace .Values.namespaceOverride | quote }}
  {{- with .Values.opengatellm-core.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  labels:
    {{- include "opengatellm-core.labels" . | nindent 4 }}
    app.kubernetes.io/component: api
spec:
  replicas: {{ .Values.opengatellm-core.replicas }}
  {{- with .Values.opengatellm-core.revisionHistoryLimit }}
  revisionHistoryLimit: {{ . }}
  {{- end }}
  selector:
    matchLabels:
      app: opengatellm-core
  {{- with .Values.opengatellm-core.strategy }}
  strategy:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  template:
    metadata:
      annotations:
        {{- include "opengatellm-core.config.checksum" . | nindent 8 }}
      labels:
        app: opengatellm-core
        app.kubernetes.io/component: api
      namespace: {{ default .Release.Namespace .Values.namespaceOverride | quote }}
    spec:
      serviceAccountName: {{ template "opengatellm-core.serviceAccountName" . }}
      {{- if .Values.opengatellm-core.priorityClassName }}
      priorityClassName: {{ .Values.opengatellm-core.priorityClassName }}
      {{- end }}
      {{- with .Values.opengatellm-core.securityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.opengatellm-core.initContainers }}
      initContainers:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- if .Values.image.pullSecrets }}
      imagePullSecrets:
      {{- range .Values.image.pullSecrets }}
        - name: {{ . }}
      {{- end }}
      {{- end }}
      containers:
        - name: opengatellm-core
          image: "{{ .Values.opengatellm-core.image.repository }}:{{ .Values.opengatellm-core.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.opengatellm-core.image.pullPolicy }}
          {{- if .Values.opengatellm-core.extraArgs }}
          args:
          {{- range $key, $value := .Values.opengatellm-core.extraArgs }}
            - -{{ $key }}={{ $value }}
          {{- end }}
          {{- end }}
          volumeMounts:
            {{- if .Values.opengatellm-core.extraVolumeMounts }}
              {{ toYaml .Values.opengatellm-core.extraVolumeMounts | nindent 12}}
            {{- end }}
            - name: config
              mountPath: /home/opengatellm-core/conf
              readOnly: true
            - name: logging-config
              mountPath: /home/opengatellm-core/logging
              readOnly: true
          env:
          - name: CONFIG_FILE
            value: "/home/opengatellm-core/conf/config.yml"
          {{- with .Values.opengatellm-core.gunicornArgs }}
          - name: GUNICORN_CMD_ARGS
            value: {{ . | quote }}
          {{- end }}
          {{- if not .Values.redis.existingRedisSecret }}
          - name: REDIS_HOST
            value: {{ printf "%s-redis" .Release.Name }}
          - name: REDIS_PORT
            value: "{{ default 6379 .Values.redis.redis.port }}"
          - name: REDIS_PASSWORD
            valueFrom:
              secretKeyRef:
                name: {{ printf "%s-redis" .Release.Name }}
                key: {{ default .Values.redis.authKey .Values.redis.sentinel.authKey }}
          {{- end }}
          {{- if not .Values.postgresql.existingPostgresSecret }}
          - name: POSTGRES_URI
            valueFrom:
              secretKeyRef:
                name: {{ printf "%s-app" .Values.postgresql.clusterName }}
                key: uri
          - name: POSTGRES_HOST
            valueFrom:
              secretKeyRef:
                name: {{ printf "%s-app" .Values.postgresql.clusterName }}
                key: host
          - name: POSTGRES_PORT
            valueFrom:
              secretKeyRef:
                name: {{ printf "%s-app" .Values.postgresql.clusterName }}
                key: port
          - name: POSTGRES_USER
            valueFrom:
              secretKeyRef:
                name: {{ printf "%s-app" .Values.postgresql.clusterName }}
                key: user
          - name: POSTGRES_PASSWORD
            valueFrom:
              secretKeyRef:
                name: {{ printf "%s-app" .Values.postgresql.clusterName }}
                key: password
          {{- end }}
          envFrom:
            {{- if .Values.existingModelSecret }}
            - secretRef:
                name: {{ .Values.existingModelSecret }}
            {{- end }}
            {{- if .Values.postgresql.existingPostgresSecret }}
            - secretRef:
                name: {{ .Values.postgresql.existingPostgresSecret }}
            {{- end }}
            {{- if .Values.redis.existingRedisSecret }}
            - secretRef:
                name: {{ .Values.redis.existingRedisSecret }}
            {{- end }}
          ports:
            - name: http
              containerPort: 8000
              protocol: TCP
          livenessProbe:
            {{- toYaml .Values.opengatellm-core.probes.liveness | nindent 12 }}
          readinessProbe:
            {{- toYaml .Values.opengatellm-core.probes.readiness | nindent 12 }}
          resources:
            {{- toYaml .Values.opengatellm-core.resources | nindent 12 }}
          securityContext:
            {{- toYaml .Values.opengatellm-core.containerSecurityContext | nindent 12 }}
{{- if .Values.opengatellm-core.extraContainers }}
{{ toYaml .Values.opengatellm-core.extraContainers | indent 8}}
{{- end }}
      {{- with .Values.opengatellm-core.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.opengatellm-core.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.opengatellm-core.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      terminationGracePeriodSeconds: {{ .Values.opengatellm-core.terminationGracePeriodSeconds }}
      {{- with .Values.opengatellm-core.dnsConfig }}
      dnsConfig:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      volumes:
        - name: config
          projected:
            sources:
            - configMap:
                name: opengatellm-core-config
        - name: logging-config
          configMap:
            name: opengatellm-core-logging-config
        {{- if .Values.opengatellm-core.extraVolumes }}
        {{- toYaml .Values.opengatellm-core.extraVolumes | nindent 8}}
        {{- end }}

