apiVersion: v1
kind: ConfigMap
metadata:
  name: opengatellm-config
  namespace: {{ default .Release.Namespace .Values.namespaceOverride }}
  labels:
    {{- include "opengatellm.labels" . | nindent 4 }}
    app.kubernetes.io/component: api
data:
  config.yml: |
    {{- include "opengatellm.calculatedConfig" . | nindent 4 }}
