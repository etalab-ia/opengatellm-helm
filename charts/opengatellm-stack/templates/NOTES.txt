# File: opengatellm-stack/templates/NOTES.txt
Thank you for installing {{ .Chart.Name }}.

Your release is named {{ .Release.Name }}.

To learn more about the release, try:

  $ helm status {{ .Release.Name }}
  $ helm get all {{ .Release.Name }}

Access URLs:
  NOTE: It may take a few minutes for all services to be in "Running" state. OpengateLLM needs to be restarted once the vLLM pod is up, so that it can pick up the new model.

  To find the public IP addresses:
    $ kubectl get svc -n {{ .Release.Namespace }} | grep LoadBalancer

  Use the "EXTERNAL-IP" to access each service:
  - API: http://<API-EXTERNAL-IP>:{{ .Values.opengatellm.opengatellm.service.port }}
  - Playground: http://<Playground-EXTERNAL-IP>:{{ .Values.playground.service.port }}
  {{- if index .Values "vllm-stack" "enabled" }}
  - vLLM Router: http://<Router-EXTERNAL-IP>:{{ index .Values "vllm-stack" "routerSpec" "servicePort" }}
  {{- end }}
