# File: opengatellm-stack/templates/NOTES.txt
Thank you for installing {{ .Chart.Name }}.

Your release is named {{ .Release.Name }}.

To learn more about the release, try:

  $ helm status {{ .Release.Name }} -n {{ .Release.Namespace }}
  $ helm get all {{ .Release.Name }} -n {{ .Release.Namespace }}

You can test the deployments using a port forward :
  $ kubectl port-forward -n {{ .Release.Namespace }} svc/opengatellm-core 8000:8000
  $ kubectl port-forward -n {{ .Release.Namespace }} svc/{{ .Release.Name }}-playground 8501:8501

Test request :
  $ curl --request GET --url http://localhost:8000/health

Or run the bootscrap script :
  $ ./bootstrap_ogl.sh