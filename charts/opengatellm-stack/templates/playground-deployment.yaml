{{- if .Values.opengatellm-stack.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: opengatellm-stack-playground
  labels:
    {{- include "opengatellm-stack.labels" . | nindent 4 }}
    app.kubernetes.io/component: playground
spec:
  replicas: {{ .Values.playground.replicas }}
  selector:
    matchLabels:
      app: opengatellm-stack-playground
  template:
    metadata:
      labels:
        app: opengatellm-stack-playground
    spec:
      {{- with .Values.playground.nodeSelector }}
      nodeSelector:
        {{ . | toYaml }}
      {{- end }}
      containers:
      - name: playground
        image: {{ .Values.playground.image.repository }}:{{ .Values.playground.image.tag }}
        imagePullPolicy: {{ .Values.playground.image.pullPolicy }}
        env:
          - name: CONFIG_FILE
            value: "/home/opengatellm/conf/config.yml"
          - name: OPENGATELLM_URL
            value: {{ .Values.opengatellm-stack.opengatellm-core.structuredConfig.playground.api_url | quote }}
          {{- if not .Values.opengatellm-stack.redis.existingRedisSecret }}
          - name: REDIS_HOST
            value: {{ printf "%s-redis" .Release.Name }}
          - name: REDIS_PORT
            value: "{{ default 6379 .Values.opengatellm-stack.redis.redis.port }}"
          - name: REDIS_PASSWORD
            valueFrom:
              secretKeyRef:
                name: {{ printf "%s-redis" .Release.Name }}
                key: {{ default .Values.opengatellm-stack.redis.authKey .Values.opengatellm-stack.redis.sentinel.authKey }}
          {{- end }}
          {{- if not .Values.opengatellm-stack.postgresql.existingPostgresSecret }}
          - name: POSTGRES_HOST
            valueFrom:
              secretKeyRef:
                name: {{ printf "%s-app" .Values.opengatellm-stack.postgresql.clusterName }}
                key: host
          - name: POSTGRES_PORT
            valueFrom:
              secretKeyRef:
                name: {{ printf "%s-app" .Values.opengatellm-stack.postgresql.clusterName }}
                key: port
          - name: POSTGRES_USER
            valueFrom:
              secretKeyRef:
                name: {{ printf "%s-app" .Values.opengatellm-stack.postgresql.clusterName }}
                key: user
          - name: POSTGRES_PASSWORD
            valueFrom:
              secretKeyRef:
                name: {{ printf "%s-app" .Values.opengatellm-stack.postgresql.clusterName }}
                key: password
          {{- end }}
        envFrom:
          {{- if .Values.opengatellm-stack.redis.existingRedisSecret }}
          - secretRef:
              name: {{ .Values.opengatellm-stack.redis.existingRedisSecret }}
          {{- end }}
          {{- if .Values.opengatellm-stack.postgresql.existingPostgresSecret }}
          - secretRef:
              name: {{ .Values.opengatellm-stack.postgresql.existingPostgresSecret }}
          {{- end }}
        ports:
          - containerPort: {{ .Values.playground.service.targetPort }}
        volumeMounts:
          - name: playground-config-vol
            mountPath: /home/opengatellm/conf
            readOnly: true
        {{- if .Values.playground.probes.readiness }}
        readinessProbe:
          httpGet:
            path: {{ .Values.playground.probes.readiness.path }}
            port: {{ .Values.playground.service.targetPort }}
          initialDelaySeconds: {{ .Values.playground.probes.readiness.initialDelaySeconds }}
          periodSeconds: {{ .Values.playground.probes.readiness.periodSeconds }}
        {{- end }}
        {{- if .Values.playground.probes.liveness }}
        livenessProbe:
          httpGet:
            path: {{ .Values.playground.probes.liveness.path }}
            port: {{ .Values.playground.service.targetPort }}
          initialDelaySeconds: {{ .Values.playground.probes.liveness.initialDelaySeconds }}
          periodSeconds: {{ .Values.playground.probes.liveness.periodSeconds }}
        {{- end }}
      volumes:
        - name: playground-config-vol
          configMap:
            name: opengatellm-stack-playground-config
{{- end }}
